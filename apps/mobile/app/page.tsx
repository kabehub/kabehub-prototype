import { SHARED_PACKAGE_MARKER } from "@kabehub/shared";

export default function HomePage() {
  // Task 3: workspace skeleton only. Real pages are migrated in later tasks (see 実装方針v5 §7).
  return (
    <main>
      <h1>KabeHub Mobile</h1>
      <p>{SHARED_PACKAGE_MARKER}</p>
    </main>
  );
}
